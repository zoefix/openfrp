# Build output
/bin/
/dist/
*.exe
*.test
*.out

# The frp comparison harness. Docker images, generated load, and one JSON per
# run — none of it ships, and the results are only meaningful on the machine
# that produced them.
/bench/

# The local three-container stack. A way to try a tunnel on a laptop, not
# something a router or a build ever reads.
#
# Anchored: deploy/ unanchored would also match internal/deploy, which is the
# SSH provisioner and very much ships.
/deploy/

# Go
vendor/

# Local dev
*.db
*.db-wal
*.db-shm
/tmp/
.env

# Editor / OS
.DS_Store
.idea/
.vscode/
