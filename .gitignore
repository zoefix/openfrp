# Build output
/bin/
/dist/
*.exe
*.test
*.out

# The frp comparison harness. Docker images, generated load, and one JSON per
# run — none of it ships, and the results are only meaningful on the machine
# that produced them.
/bench/

# Go
vendor/

# Local dev
*.db
*.db-wal
*.db-shm
/tmp/
.env

# Editor / OS
.DS_Store
.idea/
.vscode/
